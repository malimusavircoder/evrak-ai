[phases.setup]
aptPkgs = [
  "python3-pip",
  "tesseract-ocr",
  "tesseract-ocr-eng",
  "tesseract-ocr-tur"
]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port $PORT"
